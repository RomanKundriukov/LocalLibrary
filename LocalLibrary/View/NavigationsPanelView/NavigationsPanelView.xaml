<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="LocalLibrary.View.NavigationsPanelView.NavigationsPanelView"
             xmlns:viewmodel="clr-namespace:LocalLibrary.ViewModel"
             x:DataType="viewmodel:NavigationsPanelViewModel"
             xmlns:navigation="clr-namespace:LocalLibrary.View.NavigationsPanelView"
             xmlns:content="clr-namespace:LocalLibrary.View.ContentView"
             xmlns:model="clr-namespace:LocalLibrary.Model"
             Title="NavigationsPanelView">
    <!--<VerticalStackLayout
        Spacing="10"
        Padding="10">
        <Button 
            Text="Button 1"
            Clicked="Button_Clicked"/>
        <Button 
            Text="Button 2"
            Command="{Binding nav2Command}"/>
    </VerticalStackLayout>-->

    <CollectionView
        x:Name="collectionView"
        x:FieldModifier="public"
        SelectionMode="Single">
        <CollectionView.ItemsSource>
            <x:Array Type="{x:Type model:FlyoutPageItem}">
                <model:FlyoutPageItem Title="Contacts"
                                      TargetType="{x:Type content:NewPage1}"/>
            </x:Array>
        </CollectionView.ItemsSource>
    </CollectionView>
</ContentPage>