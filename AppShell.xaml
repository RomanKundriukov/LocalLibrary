<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="LocalLibrary.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:con="clr-namespace:LocalLibrary.View.ContentView"
    xmlns:local="clr-namespace:LocalLibrary"
    xmlns:nav="clr-namespace:LocalLibrary.View.NavigationsPanelView"
    xmlns:view="clr-namespace:LocalLibrary.View"
    Shell.FlyoutBehavior="Flyout"
    Shell.NavBarHasShadow="False"
    Shell.NavBarIsVisible="False">

    <ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate view:HauptSeiteView}"
        Route="MainPage" />

</Shell>
