<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="LocalLibrary.View.ContentView.StartContentView"
             xmlns:viewmodel="clr-namespace:LocalLibrary.ViewModel"
             x:DataType="viewmodel:StartContentViewModel"
             NavigationPage.HasNavigationBar="False"
             Title="Detail"
             BackgroundImageSource="background_content.png">
    <VerticalStackLayout
        VerticalOptions="Center">
        
        <Label
            Text="Добро пожаловать в домашнюю библиотеку"
            FontSize="64"
            VerticalOptions="CenterAndExpand"
            HorizontalOptions="CenterAndExpand"/>
        <!--<Button 
           Text="Content 1"
           Command="{Binding testCommand}"/>

        <Button 
            Text="nav"
            Clicked="ObjectClicked"/>-->
    </VerticalStackLayout>
</ContentPage>